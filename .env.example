# Optional PB-007 LLM relay variable names.
# Template only: do not store real provider keys in repo-local env files.
# Prefer shell/session environment variables or deployment secrets.
NIMIRUN_LLM_MODE=mock
OPENAI_ROUTE_PROPOSAL_MODEL=gpt-5.4-mini
OPENAI_API_KEY=
