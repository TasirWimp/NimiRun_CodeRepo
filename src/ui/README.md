# UI Helpers

Reusable UI helpers belong here.

Use this area for HUD, receipt panel, approval controls, and other presentation helpers that should not live directly inside a Phaser scene once they become non-trivial.
